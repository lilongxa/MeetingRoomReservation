import React from 'react';
import { Typography } from '@mui/material';

const SettingsPage = ()=>{
    return <Typography variant='h4'>Welcome to the Settings Page</Typography>;
};

export default SettingsPage;